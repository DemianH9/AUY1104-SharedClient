apiVersion: v1
kind: Service
metadata:
  name: __APP__
spec:
  type: NodePort
  selector:
    app: __APP__
    color: blue
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30090